skill:
  slug: <skill-name>
  title: <Skill Name>
  purpose: <One reusable capability>
  use_when:
    - <trigger>
  required_context:
    - <context file>
  process:
    - <step>
  checks:
    - <check>
  outputs:
    - <output>
