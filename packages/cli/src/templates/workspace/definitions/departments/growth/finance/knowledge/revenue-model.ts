import { growthKnowledge } from "../../shared-content.js";

export function revenueModelKnowledge(): string {
  return growthKnowledge("Revenue Model", "Capture how the product may generate revenue and what assumptions need validation.", ["Revenue Stream", "Customer Segment", "Billing Logic", "Suposições", "Evidence Needed"]);
}
