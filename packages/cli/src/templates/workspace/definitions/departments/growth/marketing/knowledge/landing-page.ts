import { growthKnowledge } from "../../shared-content.js";

export function landingPageKnowledge(): string {
  return growthKnowledge("Landing Page", "Draft the first landing page message, conversion goal and validation signal.", ["Hero Message", "Problem", "Oferta", "CTA", "Objeções", "Validation Signal"]);
}
